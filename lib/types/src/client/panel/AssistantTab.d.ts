import type { NovelApi } from '../api.ts';
/** Props. */
export interface AssistantTabProps {
    api: NovelApi;
}
/** The assistant conversation tab. */
export declare function AssistantTab({ api }: AssistantTabProps): any;
