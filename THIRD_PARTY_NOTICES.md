# Third-Party Notices / 第三方组件声明

本仓库（dsh-novel-forge，Apache-2.0）包含少量**移植自第三方开源项目**的种子数据与提示词素材。
源代码中不再内联这些出处（保持代码自描述），归属声明统一登记在本文件；本文件随 npm 包一同分发。

---

## 1. 小说生成种子数据与写作骨架素材

- **来源仓库**：`ExplosiveCoderflome/AI-Novel-Writing-Assistant`
- **移植基准版本**：`v0.2.3`
- **该版本许可证**：Apache License 2.0
- **版权**：Copyright 2026 ExplosiveCoderflome and contributors

### 移植范围（仅限上面这一版本）

| 本仓库位置 | 移植内容 | 数量 |
|---|---|---|
| `src/assets.ts` — `BUILTIN_STYLE_TEMPLATES` | 叙事风格模板（键名、名称、分类、标签、适用题材与各维度写法规则） | 16 套中 **8 套**沿用，另 8 套为本仓库自建 |
| `src/assets.ts` — `BUILTIN_ANTI_AI_RULES` | 反 AI 规则（键名、名称、要避免的表达问题、修正方向、命中模式） | 18 条中 **11 条**沿用（上游 12 条中 1 条未采用），另 7 条为本仓库自建 |
| `src/assets.ts` — `BUILTIN_STARTER_STYLE_PROFILES` | 起步写法档 | **4 档全部**沿用 |
| `src/assets.ts` — `BUILTIN_GENRE_LIBRARY` | 题材分类的**命名**（沿用其题材名称作为分类标签） | 14 个顶层题材中 **7 个**同名，其余自建；下层节点多为自建 |
| `src/prompting.ts` — 章节生成骨架 | 由章节写作模板派生的「必达 / 禁止 / 输出前自查」骨架与结构约束 | 1 处 |
| `src/market-radar-sources.ts` — 榜单抓取源 | 公开榜单页面的抓取源清单（URL 模板与选择器，属事实性数据） | 1 组 |

**未移植**：推进模式（`BUILTIN_PROGRESSION_MODES`，5 项）与剧情节拍（`BUILTIN_PLOT_BEATS`，9 项）
经逐条比对均不含上游文本，为本仓库自建。

除上表所列，本仓库的其余部分（中文网文工作流、道藏/大世界/编年录/剧情线、九维审稿、
DSH 集成层、规则工程、面板 UI 等）均为独立实现。

### 重要限制（发布红线）

上游项目自 **`v0.2.4` 起变更为 AGPL-3.0-only 并提供商业授权**（更早的 `desktop-beta-*` 标签为 MIT，
`v0.2.0-beta.1` ~ `v0.2.3` 为 Apache-2.0）。因此：

1. 上述移植**只以 `v0.2.3`（Apache-2.0）为来源**；
2. **不得**从 `v0.2.4` 及之后的任何版本（含当前主分支）复制代码、提示词或文案进入本仓库——
   那会与本仓库的 Apache-2.0 许可冲突；
3. 仓库自带检查脚本 `scripts/check-third-party.mjs` 会在 CI 与发布流程中拦截上游名称出现在
   代码与产物里，作为"不要重新引入外部来源"的自动化提醒。

---

## 2. 仅参考设计思路、未复制任何内容

本仓库「规则工程」部分（阶段契约、失败分级、截断声明）的实现为独立编写，思路上参考过若干同类
开源插件的公开文档与源码。这些项目**没有任何代码、文案或数据被复制进本仓库**，因此不构成本仓库的
许可义务，也不在此逐一列名。

如需追溯具体讨论与决策记录，见 `CHANGELOG.md` 与仓库提交历史。
