# Changelog

## [1.3.0-alpha] - 2026-09-13

三批能力补齐 + 内置资产全自研改写：用量可见与故障恢复、分范围导出与项目备份、故事时间线与版本回滚、张力曲线与提示词槽位。

内置种子数据与写作骨架全部改为自研表述（去外部文本依赖）：

- **改写范围**：8 套叙事风格模板、11 条反 AI 规则、4 档起步写法档、章节生成骨架，以及推进模式 / 剧情节拍里的冲突形态与信号清单——共约 2.3 万字符的**表达文本**全部改由本项目自行撰写（`name` / `description` / `analysisMarkdown` / 各段 `summary` / 规则条目 / `avoid` / `fix` / 骨架行文）。改完用上游原文件逐条复核：源码中 273 条中文表述**零逐字重合**。
- **保持不变的（功能标识）**：模板 `key` 与 `templateKey` 引用、`detectPatterns`（扫描实际匹配的短串）、全部枚举值与布尔/数值开关、`defaultAntiAiRuleKeys`、题材树与推进模式、榜单抓取源的地址与选择器。既有项目的写法绑定与审稿规则按 key 匹配，**不受影响**。
- **影响面**：模型看到的风格指引与骨架措辞变了，同一章的生成结果不会与旧版逐字一致（风格意图与约束项逐条保留）。
- **长期约束**：`scripts/check-third-party.mjs`（`pnpm check:third-party`，CI 与发布流程均执行）现在扫描 `src/ tests/ scripts/ README.md CHANGELOG.md` 与全部配置，命中外部项目名 / 组织名 / 外部符号名 / 不相容许可证标记，或把内容标注成外部来源的措辞即失败。**今后的原则：只参考同类工具的功能结构与划分，不搬运任何文本，一律转写成本项目自己的说法。**

第一批「向成熟生产系统补齐」的四项能力（A1–A4）：

- **A1 Token / 耗时记账**：实况帧新增 `usage`（输入 / 输出 / 思考 / 缓存 token）与 `elapsedMs`、`firstTokenMs`；`beginLiveCall` / `endLiveCall` / `markFirstToken` 统一打点，覆盖 `complete()` 与正文生成 / 修订 / 润色 / 助手对话全部 LLM 调用（此前实况只覆盖 `complete()`，正文生成完全不可见）。`/status.usage` 返回本次运行（进程内）汇总：调用次数、失败数、各类 token、模型耗时、未上报用量的调用数，以及**按用途分组**；面板实况区新增「本次运行」累计条与「重新计数」。
- **A2 查看实际发送的 Prompt**：每次调用保存 system + user（环形保留最近 40 次、单条上限 6 万字符），新增 `GET /llm-live/prompt?sessionId=`；实况卡片提供「查看 Prompt」按钮，弹层可切换 User / System 两个页签并显示总字符数与截断标记。
- **A3 分范围导出 + 项目备份 JSON**：`/export` 新增 `scope`：`book`（整本正文）/ `settings`（总纲·道藏·大世界·写作资产·卷首语）/ `plan`（卷 + 章节计划，含必达·硬事实·钩子·义务合约）/ `characters`（角色库 + 人物志）/ `review`（逐章审稿 + 作者复盘 + 暗线 + 剧情线 + 待办）/ `project`（完整项目 JSON，带 `schema` 与导出时间，用于备份迁移）；设置页导出卡片改为「范围下拉 + 对应格式按钮」。
- **A4 备用模型与失败恢复**：新增 `fallbackModel` 配置（设置页「备用模型（故障自动切换）」）；新增 `src/llm-retry.ts` 定义恢复规则——只对网络/超时/限流/服务端/额度类失败切换，用户取消与参数错误不切换；备用模型也失败时抛**主模型原始错误**保留第一现场。`complete()` 与助手对话按此重试一次；正文生成 / 修订 / 润色**仅在「一个字都没产出」时**才换模型重试，杜绝重复生成。
- 工程：新增 `src/llm-retry.ts`、`tests/llm-retry.test.ts`（11 例，覆盖可重试判定、切换条件、原始错误保留、未配置时不切换、onSwitch 回调），套件 → 69 个用例。

第二批「长篇一致性的地基」两项能力（B1 故事时间线 / B3 历史版本回滚）：

- **B1 故事时间线**：新增 `src/timeline.ts`（纯逻辑：归一化、按章排序、注入渲染、规则初筛）与引擎抽取/复核（`extractTimelineForChapter` / `reviewTimeline`）。出章后自动抽取「故事内时间 + 地点 + 在场角色 + 事件」（`autoTimeline` 默认开，失败不阻断出章，生产单同样生效）；写作与修订提示词注入**本章之前**的时间线锚点（回写旧章不被剧透，超过 12 条带显式截断声明）；规则初筛直接抓三类错：同章内「自报 order 与叙述顺序打架」、跨章**时间倒流**、**地点瞬移**；AI 复核补季节/年龄/回忆混淆等规则抓不到的问题。面板新增侧柜「故事时间线」：按章分组浏览、选章抽取、一键检查矛盾、就地修正（改动标记 manual）与删除。
- **B3 章节历史版本（快照）**：新增 `src/snapshots.ts`（`snapshots/` 目录 + `index.json` 索引）。**重新生成**、**采纳草稿前**、**助手直接修订前**都会自动留存旧稿；新增 `/snapshot` API（list / create / restore / remove / prune，每章默认保留 20 份）；**回滚前会先给当前稿留一份，所以回滚本身也可撤销**。章节工作台新增「历史版本」页签：按时间倒序列出（含留存原因与字数）、一键回滚、手工存档。
- 工程：新增 `tests/timeline.test.ts`（11 例）与 `tests/snapshots.test.ts`（8 例，真实文件系统），套件 69 → 88 个用例。

第三批（B2 张力曲线 / B4 提示词槽位）：

- **B2 张力曲线**：新增 `src/tension.ts`（本项目自行设计的参考形状：递进爬升 / 悬疑加压 / 波浪呼吸 / 前紧后稳 / 平稳推进 + 自定义）与逐章张力记录 `chapter.tension`（目标）、`chapter.tensionActual` / `review.tension`（审稿评分）。写作提示词新增「本章张力」块（目标值 + 与上一章走向 + 参考曲线位置）；章节规划要求模型给出 `tension` 目标；审稿 JSON 新增 `tension` 字段（张力强度，非质量分）。规则核对抓四类**曲线级**问题——实际与目标偏差 >25、连续 4 章几乎同值（平台期）、连续 3 章高位 ≥80 无呼吸、连续 5 章低位 <40——这些是单章审稿看不见的。面板新增侧柜「张力曲线」：SVG 折线（虚线参考 / 实线目标 / 绿点实际）、点击任意一章即可改目标张力。
- **B4 提示词槽位**：新增 `src/prompt-slots.ts`，只开放四个**安全**槽位（文风补充 / 反 AI 补充 / 对话补充 / 章末钩子偏好），随项目保存、有字符上限并在超长时显式声明截断。槽位只**追加偏好**，渲染时固定声明"与道藏、写作红线、内容合规红线冲突时一律以后者为准"——规则工程、合规红线与阶段契约由宿主锁定，槽位碰不到。面板新增侧柜「提示词槽位」，逐项保存 / 清空（清空即恢复默认）。
- 工程：新增 `tests/tension.test.ts`（16 例）与 `tests/prompt-slots.test.ts`（10 例），套件 98 → 124 个用例。

## [1.2.1-alpha] - 2026-09-13

发版流程与安装文档修正（无功能变更）。

- **`latest` 不再停在首个版本**：本线所有版本都是 `-alpha` 预发布，`npm publish --tag alpha` 不会动 `latest`，裸装（`npm i @waterwx/dsh-novel-forge`）会一直拿到 1.0.0-alpha。发布脚本现在在 publish 后显式 `npm dist-tag add … latest`，并已把 `latest` 手动同步到 1.2.0-alpha（将来发正式版时删掉这段即可）。
- **GitHub Release 标记 prerelease**：版本号含 `-` 时创建/更新 release 会带 `prerelease: true`。
- **发布前跑测试**：脚本第 3 步在 typecheck/样式校验/build 之后加 `pnpm test`，与 CI 对齐——带着失败用例发布过一次就收不回来（npm 版本号不可复用）。
- **安装文档显式带 `@alpha`**：README 中英文安装命令由裸包名改为 `@waterwx/dsh-novel-forge@alpha`，避免 registry 标签或缓存导致装到旧版本。

## [1.2.0-alpha] - 2026-09-13

规则工程 + 切书串书修复：阶段契约贯通到面板与助手、fail-stop 错误分级、截断显式化；助手不再跨书串台。

**切书串书修复**（助手在多书之间错乱）：

- **助手发送补齐 bookId**：面板里 80 处书级请求都带 `bookId`，唯独助手的发送那一处没带，宿主因此退回「全局 active 书」。平时两者一致看不出问题，一旦切书与请求并发，就会出现「在 A 书界面聊天、回答写进 B 书」。现抽出 `withBookIdBody()` 与既有的 `withBookId()` 对称，`postJson` 与 `assistant()` 共用，并把这条不变量写进注释。
- **助手对话按书重建**：助手聊天记录是**按书存档**的（每本书目录各存一份 `novel-assistant.jsonl`），但面板里的对话状态没有按书隔离——切书后屏幕上仍留着上一本的聊天，而新消息已发到另一本书。现给 `AssistantTab` 加 `key={bookKey}`（与 `api.setCurrentBook` 同步的绑定书 id），切书即重建并重新拉取本书历史。
- 新增 `tests/client-book-scope.test.ts`：断言发送/读取历史都按当前书路由、切书后按新书发送、未绑定时保留宿主 active 兜底。

规则工程三件事（借鉴同类 DSH 小说插件的规则设计，不动功能面）：

- **阶段契约**：新增 `src/stage-contract.ts`，宿主按项目真实状态算出「当前阶段 → 本轮允许的动作」（开书/立设定/排章节/等待/逐章编译/审稿/修订/定稿导出）。阶段契约注入助手系统提示词；作者只说了「继续/下一步」而没指定对象时，阶段白名单成为唯一命令，阶段外写操作一律拒绝并说明理由（作者显式点名时仍以作者指令为准）。
- **fail-stop 错误分级**：新增 `src/action-guard.ts`。参数/契约类失败（参数缺失、片段未找到、章节不存在、非法取值）禁止原样重复同一调用，第二次即停止本轮；瞬时故障（网络/模型中断/磁盘临时不可用）允许重试一次。动作标签 JSON 解析失败给一次修正机会，再失败即停。系统提示词同步加入「失败即停」「状态诚实（拿到成功结果前不得声称已保存）」两条规则。
- **截断显式化**：`/status` 新增 `truncations` 声明（编年录只回最近 80 条、slim 瘦身范围）；规划注入的编年录、已发生事件清单、知识库检索命中均带「共 N 条，此处只取 M 条」说明；助手快照的编年录显示总数与省略条数。消费方不再能把「看到的」当成「全部」。
- **阶段契约贯通**：`/status` 新增 `stage`（`StageInfo`），宿主只算一次，面板角标、助手提示词、外部自动化共用同一结论；总编台主行动卡显示「阶段 · X」角标，`wait` 阶段（有章节正在生成）改为「正在生成中…」而不是继续推荐批量生成；助手提示词新增「动作可见性」段（本阶段可直接执行 / 阶段外仅作者点名可执行 / 只读工具始终可用），模型不必靠试错发现哪些动作会被拒绝。
- **动作裁剪（硬约束）**：动作说明书从提示词字面量提取为 `ASSISTANT_TOOL_DOCS`，新增 `renderToolDocs(docs, stage, strict)`。作者只说了「继续/下一步」时（`strict=true`），阶段外写操作的说明书**从提示词里真正移除**，并列出「本轮不可用」清单——提示词可见集与执行层白名单完全一致，模型不再有机会试错撞守卫；作者点名时（`strict=false`）说明书全量照给，作者终审权不受影响。只读工具在任何阶段都不裁剪。
- **助手面板阶段条**：助手 Tab 顶部显示「当前阶段 · X / 建议下一步 / （本阶段由面板操作推进）」，与工作台角标同源（`/status.stage`）。
- **前置条件失败显式分级**（真机验证暴露）：引擎里 13 处前置条件（`章节 N 不在计划中`、`章节 N 的正文文件不存在`、`正文中未找到要修改的片段`、`还没有已写章节` 等）原本是普通 `Error`，会被兜底正则误判成瞬时故障而多跑一次无效重试。现改为在抛出点标记 `NovelActionError('contract')`，并补充 `/不在计划中/`、`/正文文件不存在/`、`/还没有已写章节/`、`/没有可分析的已写章节/`、`/请先/` 兜底特征。
- 工程：新增 `vitest.config.ts`（此前从大仓内运行时会被上层 workspace 配置吞掉测试）；新增 `tests/action-guard.test.ts`、`tests/stage-contract.test.ts`、`tests/status-route.test.ts`（路由级：阶段、truncations、loopback 403、临时 HOME 隔离）、`tests/assistant-prompt.test.ts`（可见性 + 裁剪），套件 6 → 58 个用例。

## [1.1.0-alpha] - 2026-09-08

第二版 alpha：工作台重构 + 输出目录迁移 + 本书级参数。

- 界面：总编台顶栏/右滑抽屉架构（顶栏按钮 + 写作章节索引 + 设定/资产/工具抽屉化 + 章节独立全页三栏工作台）；章节工作台「审稿 → 勾选意见 → 修订 → 对比 → 应用/放弃」闭环；总纲结构化阅读（层级/段落/卷分组）；书架统计卡成为唯一筛选入口；字号整体上调一档；删除旧左导航与重复入口，侧柜收敛为总编台内嵌格。
- 输出目录迁移：设置页「更改位置」一键搬迁，复制 → 书架重指向 → 默认目录同步 → 最后删源，数据零丢失；生产单运行时拒绝迁移。
- 本书级参数：`novel-project.json.bookSettings` 按书覆盖 provider/model/reasoningEffort/chapterChars/maxTokens/审稿开关，未设回退全局；总编台「本书参数」抽屉 + 模型快切 + 恢复全局默认。
- 架构/稳定性：docx 文本提取拆共享核心（`docx-core.ts`）消除双实现漂移；新增输出目录迁移工具；三批次死代码/未用符号清理（80 处）并开启 `noUnusedLocals`/`noUnusedParameters`；损坏项目/资产文件静默回退改为服务端留痕日志；`dsh-*` 依赖钉版 `0.1.2-alpha.3` 避免 caret 漂移。
- 发布质量：typecheck / build / 主题尺寸 / 字号档位门禁通过；删除 demo 样稿与设计文档，仓库只保留插件本体。

## [1.0.0-alpha] - 2026-09-06

首个 alpha 发布：**纯文本小说创作**工作台。

- 开书（大纲 docx / 粘贴，书名自动识别）→ 立项入书架。
- 设定：道藏提炼（人设/世界观/金手指规则/写作红线）、大世界（境界/区域/势力）、写作资产（题材/推进/笔法帖/文戒/心法）。
- 规划：卷计划 + 结构化章节计划（本章目标/剧情要点/爽点·钩子/结尾钩子），已有章节自动进入续写模式。
- 逐章编译：LLM 生成 3000-4000 字正文并保存为 Markdown；自动摘要 + 编年录事实库；相关事实全量检索。
- 审校：九维 AI 审稿、按意见修订 / 局部改 / 去 AI 味润色、审稿问题勾选修复、作者复盘。
- 一致性：全书质检（矛盾清单）、敏感词检查、剧情线 / 角色库（纯文本+知情度）/ 人物志 / 编年录维护。
- 辅助：AI 编辑 Agent、知识库 RAG、题材雷达→灵感、自动编辑、拆书 / 创意灵感、生产单（批量连写 + 断点续跑）、改编模式。
- 产出：全本导出 TXT / MD（含卷首语与封面）。
- 体验：创作工作流仪表盘、三套主题玻璃 UI、活动输出控制台、并发安全保存。
