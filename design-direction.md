# 小说工坊 · 设计总纲「墨案 InkDesk」

> 唯一设计基准。结构基准 = `demo-full.html`（书架→总编台）+ `demo-subpages.html`（子页布局）。
> 视觉基准 = 本文档定义的「墨纸 / 纸白」双档 token 表。
> 以前 4 套材料皮肤（macos / neumorph / clay / endfield）**删除**，不再参与。密度档与性能 lite 档保留。

---

## 0. 一句话定位

把一本正在被 AI 抄写的书，当成**夜里编辑部案头的一块活版**——外面是纸、墨、朱砂、标尺；里面是精密仪表。

你在**总编**位，AI 是替你赶稿的排字工。界面要让人感到"这是一张有年份的编辑桌"，而不是"又一个 SaaS 控制台"。

## 1. 三条红线（每次改动都自检）

1. **界面不用 emoji**，状态不用颜文字：一律线性图标 + 文字标签；AI 能力用「AI 微签」，不用 ✨。
2. **强调色一屏只出现一次**（当前行动 / 主 CTA），其余靠中性层级说话。
3. **文字先于装饰**：先定字号、行高、行宽、留白；再谈圆角、阴影、渐变。

## 2. 基调：一套主题，两档模式

删除 `data-nf-theme='macos|neumorph|clay|endfield'` 四张材料皮肤。只保留一档主题，下辖两档模式，由 `data-nf-mode` 切换：

| 模式 | 命名 | 底色 | 气质 |
|---|---|---|---|
| 暗 | **墨纸**（默认） | 暖墨 `#17140f` | 夜编辑室 · 冷灰都市悬疑《雾都手记》 |
| 亮 | **纸白** | 纸白 `#f6f3ec` | 白日编辑部 · 出版物可读 |

**纸白不是墨纸的反相**，是重配一版暖纸色。两档共用同一套语义色与排版语言。

> 保留：密度档 `data-nf-density`、性能 lite 档（与颜色无关）。

## 3. token 表（`--nf-*`，两档各给一版）

> 纪律：组件与 JSX **禁止写死十六进制**；所有颜色走 `--nf-*` 语义 token。

### 3.1 表面（面 0/1/2 三级）

| token | 墨纸（暗） | 纸白（亮） | 用途 |
|---|---|---|---|
| `--nf-bg` | #17140f | #f6f3ec | 画布底色 |
| `--nf-bg-raise` | #201c16 | #fcfaf6 | 面 1 卡表面 |
| `--nf-bg-inset` | #2a241c | #efe9dc | 面 0/内嵌/操作行 hover |
| `--nf-bg-deep` | #100e0a | #e9e2d4 | 顶部条/表头深色带 |
| `--nf-line` | rgba(242,233,220,.12) | rgba(38,34,27,.12) | 发丝边 |
| `--nf-line-2` | rgba(242,233,220,.05) | rgba(38,34,27,.05) | 分隔线 |

### 3.2 文字

| token | 墨纸 | 纸白 |
|---|---|---|
| `--nf-text` | #eee7da | #26221b |
| `--nf-text-dim` | #b3a78f | #6f665a |
| `--nf-text-faint` | #8a7f6c | #9c9284 |

> 对比度要求：正文/说明文字 ≥ 4.5:1；大号/UI ≥ 3:1。`--nf-text-faint` 只用于给次要装饰，不用在 12px 正文。

### 3.3 语义色（四件套 + 角色色）

| token | 墨纸 | 纸白 | 语义 |
|---|---|---|---|
| `--nf-ok` | #79b372 | #5f9656 | 已过审 / 成功 |
| `--nf-warn` | #e3a52e | #c98a1f | 待审 / 提醒 |
| `--nf-bad` | #d95753 | #b94a41 | 待修订 / 错误 |
| `--nf-idle` | #5c5344 | #c9c0af | 待生成 / 未开始 |
| `--nf-ai` | #d98a2b | #c07c22 | AI 产出 / 机器在跑 |
| `--nf-human` | #4aa3d9 | #2f6fa3 | 需人裁决 / 命令 |
| `--nf-influence` | #d98a2b | #c07c22 | 主 CTA / 当前行动 |

> 紫色 `#5856d6` **移除**。`--nf-influence`（朱砂红──若走红章可改 `--nf-seal`）为**唯一强调色**，一屏一次，只给"该你拍板"的动作。

### 3.4 排版档位（在 `:root`，主题无关）

| 档 | 值 | 用途 |
|---|---|---|
| display | 22–28 | 页面大标题 / 书名《》 |
| title | 16 / 14.5 | 卡片标题 / 章节标题 |
| body | 13 | 正文 / 列表主体 |
| meta | 12 | 说明 / 时间戳 / 路径 |
| caption | 11 | 徽标 / 表格头 / eyebrow |

**衬线 = 书卷感**（`--nf-font-display`，宋体系）：书名《》、卷名、章节大编号、空态标题。禁加粗。
**mono + tabular**：章节号、字数、进度、路径（`font-variant-numeric: tabular-nums`），总数固定列宽右对齐。
CJK 正文 line-height ≥ 1.6；长正文行宽 ≤ `--nf-measure 720px`；英文 eyebrow 大写 + `letter-spacing:.12em`。

### 3.5 层次 / 动效

- 半径 `--nf-r: 12px`；内嵌半径比外层小。
- 阴影只给浮窗与"正在发生"的对象；内容承载面**不发光**。
- 动效只用 `opacity`/`transform`；`prefers-reduced-motion` 全局关闭；焦点态「2px 偏移焦点环」。

## 4. 签名元素（这套 UI "只能是它"）

1. **稿纸排版**：阅读 / 大纲 / 道藏承载面铺一层极淡的 8pt 格纸横线（低透明度，只出现在"读书/看设定"的面）。
2. **红章 = 人的裁决**：唯一高饱和朱砂只给"要你拍板"（判过 / 修订 / 主 CTA）。
3. **AI 微签**：AI 能力 = 名称旁一枚 12px 半透明 `AI` mono 微签；不加 ✨、不加"AI ×××"帽。
4. **衬线大数字**：章号 / 统计 / 进度用 Display 衬线 + mono 精确小数，扫读即"书"。

## 5. 每屏一个焦点

| 页面 | 焦点 | 说明 |
|---|---|---|
| 书架 | 打开某本书 | 一本书一张"面 1"卡，状态/进度收进卡内一行，不叠徽章 |
| 总编台 | 主 CTA"让 AI 继续生产" | rail / poolbar 是它压制的仪表 |
| 审稿台 | 当前这一章 | 逐个"开庭"，不是徽章齐飞 |
| 大纲/道藏 | 内容本身 | 阅读列 720px，稿纸纹，无多余卡 |

## 6. 页面骨架（对齐 demo-full）

- 顶部**分组导航**（创作 / 灵感·策划 / 资产 / 设置），无左侧大导航。
- 作者级 = 跨书工作台；进入某本书 = **总编台**（`shelf -> workspace`）。
- 总编台：topbar(章节工作台·生产单·侧柜·AI 进度) → **可折叠 AI 进度**(当前任务+进度条+最近动态) → **四常驻指标**(今日产出/已过审/待审/待生成) → 主区(rail 工序轨道 + poolbar 状态池 + cmd 主命令 + 侧柜) → 底部信号一行。
- 子页面照 `demo-subpages.html` 补齐（大纲/道藏/大世界/角色库/编年复盘/写作审稿/章节计划/生产单/剧情线伏笔/自动编辑/拆书分析/简介封面/知识库/创作资产）。

## 7. 表面三级（替代"套卡"）

- 面 0 画布：无边框纯底色。
- 面 1 卡：浅面 + 发丝边，只在抬升/悬停给阴影。
- 面 2 操作行：无边框，hover 才有浅面。
- 实体之间用面 1 卡；卡内从属关系用分隔线 + 缩进，**不再内嵌面 1 卡**。
- 行操作默认隐藏/置灰，hover 或键盘聚焦才显现。

## 8. 组件规范（落到代码）

- 建 `PageHeader` 统一页面头：icon 16 单色 + 标题(无 emoji) + meta + 主/次按钮，下加发丝分隔。
- 状态徽章 = 色 + 文字（+ 可选 icon）三通道，不单靠边框色。
- 数字 tabular；章节号衬线；序号列固定宽右对齐。
- 行动按钮表：主 CTA(朱砂) / 次按钮(中性) / AI 动作(琥珀微签) / 人的裁决(蓝)。
- 空态 / 加载(骨架) / 错误 / 禁用：生产台尤其要补齐（0 章待生成、LLM 报错）。

## 9. 验收（"不像 AI 做出来的"测试）

- [ ] 无 emoji 图标；书名用《》衬线；AI 用微签不用 ✨
- [ ] 一屏主强调 ≤1；无"accent 框 + ✨ 满屏"
- [ ] 正文 line-height ≥1.6；12px 不做正文；数字 tabular
- [ ] 无三层以上描边嵌套；从属关系用分隔线
- [ ] 紫色不再出现；语义色全走 token
- [ ] 焦点环可见、键盘可走完全流程
- [ ] 墨纸 / 纸白两档、3 密度、lite 档全部过一遍
- [ ] 两档对比度达标（正文 ≥4.5:1）

## 10. 落地状态（实现对照）

| 设计项 | 实现 |
|---|---|
| 暖墨/纸白双档 token | ✅ `panel.module.css` 基座 + 「强制浅/深」覆盖 + 总编台 `--wf-*` 全部别名到 `--nf-*` |
| 删四材料主题 | ✅ 功能(TSX 状态/下拉/Endfield 删除) + CSS 物理删除 49 块 + 注释清理 |
| 去 emoji / AI 微签 / 去帽 | ✅ `AiTag.tsx` + 全仓去 emoji(0 残留) + 70 处"AI ×××"去帽 |
| 总编台 dashboard | ✅ `workflow` tab 已有 rail/poolbar/cmd/侧柜；补**四常驻指标**；全接 `--nf-*` 双档 |
| PageHeader / 衬线 / tabular | ✅ `PageHeader.tsx` + `.numSerif/.tabular`；用于书架/设置/章节工作台/生产单/作者级 |
| 三级表面 + 行 hover 渐显 | ✅ `.surface0/.surfaceCard/.surfaceRow/.surfaceRowAction` + `.wfAct` 行 hover 渐显 |
| 稿纸纹 | ✅ `.docsSurface`；用于知识库阅读面、大纲编辑面 |
| 全量验证 | ✅ typecheck / check:styles / audit-contrast(FAIL 0) / build 全绿；紫·主题·emoji 残留 0 |

> 剩余为可选打磨（大纲/审稿页头进一步用 PageHeader、个别视图嵌套卡再降、稿纸纹铺到道藏/世界），非设计缺失。
