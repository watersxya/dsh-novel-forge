# 小说工坊 UI 设计方案说明

## 1. 设计结论

小说工坊应拆成两层，而不是让书架页里混入总编台。

1. **作者首页 = 跨书工作台**
   - 顶部使用分组导航：`创作 / 灵感·策划 / 资产 / 设置`
   - 书架只负责管理书、筛选状态、新建和导入
   - 其他作者级功能：`改编模式、题材雷达、创意灵感、书分析、作者资产库、全局写作资产库、设置`

2. **进入某本书 = 总编台**
   - 从书架打开一本书后，才进入这本书的总编台
   - 总编台顶部按钮：`章节工作台、生产单、侧柜、AI 进度`
   - 总编台主区域常驻四个指标：`今日产出、已过审、待审、待生成`

3. **AI 进度改成顶部可折叠模块**
   - 不单独占导航，也不常驻占空间
   - 点 `AI 进度` 展开，再点一下收起
   - 展开后只显示当前任务、进度条和最近动态
   - 指标不放在 AI 进度里，而是放在总编台主区域顶部

4. **子页面继续保留**
   - 大纲、道藏、大世界、角色库、编年/复盘、写作与审稿
   - 卷/章节计划、生产单
   - 剧情线/伏笔、自动编辑、拆书分析
   - 简介/封面、知识库、创作资产

## 2. 本地样张

当前仓库里已有两个静态样张：

- `demo-full.html`：书架 → 总编台完整单页流程稿
- `demo-subpages.html`：所有子页面布局稿

`demo-full.html` 是最终视觉基准，实际代码逐块对齐。

## 3. 视觉方向

- 墨纸暗色作为默认主基调
- 书名、章节编号使用衬线字体
- 数字、章节号、路径使用 mono / tabular 数字
- 语义色与状态色统一，避免 emoji 当图标
- 每屏只保留一个主要强调动作
- 内容区使用面、卡片、列表行三级层次，减少卡片套卡片

## 4. 实施步骤

### 第 1 步：视觉底座

把 `demo-full.html` 里的暗色、字体、间距、语义色、卡片层级整理成 CSS token。

落地文件：

- `src/client/panel/panel.module.css`

### 第 2 步：改作者首页

调整作者首页结构，去掉左侧大导航，改成顶部分组导航。

涉及文件：

- `src/client/panel/AuthorHome.tsx`
- `src/client/panel/ShelfView.tsx`

需要接入的现有页面：

- `AdaptModeView.tsx`
- `MarketRadarView.tsx`
- `IdeaInspirationView.tsx`
- `BookAnalysisView.tsx`
- `AuthorAssetsView.tsx`
- `GlobalAssetLibraryView.tsx`
- `SettingsView.tsx`

### 第 3 步：改书架页

书架页保留以下内容：

- 搜索书名 / 简介
- 总书数、进行中、已完结、未开书
- 全部、进行中、已完结、未开书筛选
- 书卡状态、更新时间、进度
- 新建一本书
- 导入已有小说

### 第 4 步：改总编台

把书架到工作台做成明确的 `view` 切换：

```text
shelf -> workspace
```

总编台顶部按钮：

- 章节工作台
- 生产单
- 侧柜
- AI 进度

总编台主区域顶部增加四个常驻指标：

- 今日产出
- 已过审
- 待审
- 待生成

涉及文件：

- `src/client/panel/NovelPanel.tsx`
- `src/client/panel/panel.module.css`

### 第 5 步：拆出 AI 进度组件

`AI 进度` 变成总编台顶部按钮：

- 默认收起
- 点击展开
- 再次点击收起
- 展开内容只包含当前任务、进度条和最近动态

### 第 6 步：补齐子页面

按照 `demo-subpages.html` 补齐以下页面：

- 大纲
- 道藏
- 大世界
- 角色库
- 编年/复盘
- 写作与审稿
- 章节计划
- 生产单
- 剧情线/伏笔
- 自动编辑
- 拆书分析
- 简介/封面
- 知识库
- 创作资产

## 5. 验收方式

拿 `demo-full.html` 作为最终视觉基准，完成一个页面就截图对比：

- 信息层级是否一致
- 间距是否统一
- 顶部导航是否稳定
- AI 进度是否可展开和收起
- 运行指标是否常驻在总编台顶部

